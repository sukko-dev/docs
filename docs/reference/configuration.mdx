---
title: Configuration Reference
description: All environment variables across Sukko services
---

# Configuration Reference

All configurable environment variables across Sukko services. Values are set via `env:` struct tags in Go config files — the `envDefault` is the source of truth for defaults.

## Shared (Base)

| Variable | Type | Default | Description |
|----------|------|---------|-------------|
| `LOG_LEVEL` | string | `info` |  |
| `LOG_FORMAT` | string | `json` |  |
| `ENVIRONMENT` | string | `local` | Environment — deployment identity label, used for Kafka topic namespace, consumer group naming, and safety guards. Free-form: any string works as deployment identity. Sukko uses: local \| dev \| stg \| prod by convention. |
| `SUKKO_LICENSE_KEY` | string | — | LicenseKey is the signed Ed25519 license token that determines the edition (Community/Pro/Enterprise). Empty = Community. Parsed by license.Manager at startup. The editionManager field lives on each service config (ServerConfig, GatewayConfig, ProvisioningConfig) — not here — to avoid BaseConfig depending on the license package. |
| `OTEL_TRACING_ENABLED` | bool | `false` | Tracing (OpenTelemetry) — cold-path only, disabled by default. |
| `OTEL_EXPORTER_TYPE` | string | `otlp-grpc` |  |
| `OTEL_EXPORTER_ENDPOINT` | string | `localhost:4317` |  |
| `PPROF_ENABLED` | bool | `false` | Profiling — pprof endpoints and Pyroscope continuous profiling, disabled by default. |
| `PYROSCOPE_ENABLED` | bool | `false` |  |
| `PYROSCOPE_ADDR` | string | `http://localhost:4040` |  |
| `AUTH_MODE` | string | `required` | AuthMode is the authentication model. Only "required" is supported. Kept temporarily for backward compatibility with deployments that set AUTH_MODE=required. Extensible to "public-read" in the future (anonymous subscribe for public channels). |
| `PROVISIONING_GRPC_ADDR` | string | `localhost:9090` |  |
| `HTTP_READ_TIMEOUT` | time.Duration | `15s` |  |
| `HTTP_WRITE_TIMEOUT` | time.Duration | `15s` |  |
| `HTTP_IDLE_TIMEOUT` | time.Duration | `60s` |  |
| `KAFKA_TOPIC_NAMESPACE_OVERRIDE` | string | — | KafkaTopicNamespaceOverride overrides ENVIRONMENT for Kafka topic naming only. If empty, defaults to normalized ENVIRONMENT value via kafka.ResolveNamespace(). NOT allowed in production — startup validation blocks this. |
| `VALID_NAMESPACES` | string | `local,dev,stag,prod` | ValidNamespaces is a comma-separated list of allowed topic namespace prefixes. |
| `MESSAGE_BACKEND` | string | `direct` | valid: direct, kafka — applies to ws-server and tester only (not gateway or provisioning) |
| `KAFKA_BROKERS` | string | `localhost:19092` | Kafka Configuration (only used when MESSAGE_BACKEND=kafka) |
| `KAFKA_SASL_ENABLED` | bool | `false` | Kafka Security - SASL Authentication  For connecting to managed Kafka/Redpanda services that require authentication. When KafkaSASLEnabled=false (default), connects without authentication (local dev).  Supported mechanisms: - scram-sha-256: SCRAM-SHA-256 (recommended, widely supported) - scram-sha-512: SCRAM-SHA-512 (stronger, less common) |
| `KAFKA_SASL_MECHANISM` | string | — | scram-sha-256 or scram-sha-512 |
| `KAFKA_SASL_USERNAME` | string | — |  |
| `KAFKA_SASL_PASSWORD` | string | — |  |
| `KAFKA_TLS_ENABLED` | bool | `false` | Kafka Security - TLS Encryption  For encrypted connections to Kafka. Required for most managed services. When KafkaTLSEnabled=false (default), connects without TLS (local dev).  KafkaTLSInsecure: Skip server certificate verification (NOT for production) KafkaTLSCAPath: Path to CA certificate for server verification |
| `KAFKA_TLS_INSECURE` | bool | `false` |  |
| `KAFKA_TLS_CA_PATH` | string | — |  |
| `DATABASE_URL` | string | — |  |

## Gateway

| Variable | Type | Default | Description |
|----------|------|---------|-------------|
| `GATEWAY_PORT` | int | `3000` | Server settings |
| `GATEWAY_READ_TIMEOUT` | time.Duration | `15s` |  |
| `GATEWAY_WRITE_TIMEOUT` | time.Duration | `15s` |  |
| `GATEWAY_IDLE_TIMEOUT` | time.Duration | `60s` |  |
| `GATEWAY_BACKEND_URL` | string | `ws://localhost:3005/ws` | Backend ws-server connection |
| `GATEWAY_DIAL_TIMEOUT` | time.Duration | `10s` |  |
| `GATEWAY_MESSAGE_TIMEOUT` | time.Duration | `60s` |  |
| `GATEWAY_MAX_FRAME_SIZE` | int | `1048576` | 1MB = protocol.DefaultMaxFrameSize |
| `GATEWAY_PER_TENANT_CHANNEL_RULES` | bool | `false` | Per-tenant channel rules (Feature Flag) When enabled, channel permissions come from per-tenant rules via gRPC streaming |
| `GATEWAY_FALLBACK_PUBLIC_CHANNELS` | []string | `*.metadata` | Fallback channel rules (when tenant has none configured) |
| `REQUIRE_TENANT_ID` | bool | `true` | Multi-tenant settings |
| `DEFAULT_TENANT_CONNECTION_LIMIT` | int | `1000` |  |
| `TENANT_CONNECTION_LIMIT_ENABLED` | bool | `false` |  |
| `GATEWAY_PUBLIC_PATTERNS` | []string | `*.trade,*.liquidity,*.metadata,*.analytics,*.creation,*.social,all.trade,all.liquidity,all.metadata,all.analytics,all.creation,all.social` | Permissions - channel patterns Patterns support wildcards: *.trade matches BTC.trade, *.trade.* matches BTC.trade.user123 Aggregate channels (all.trade, all.liquidity, etc.) allow subscribing to ALL events of a type |
| `GATEWAY_USER_SCOPED_PATTERNS` | []string | `*.balances.{principal},*.trade.{principal},balances.{principal},notifications.{principal}` |  |
| `GATEWAY_GROUP_SCOPED_PATTERNS` | []string | `*.community.{group_id},community.{group_id},social.{group_id}` |  |
| `GATEWAY_RATE_LIMIT_ENABLED` | bool | `true` | Rate limiting per principal |
| `GATEWAY_RATE_LIMIT_BURST` | int | `100` |  |
| `GATEWAY_RATE_LIMIT_RATE` | float64 | `10.0` |  |
| `GATEWAY_PUBLISH_RATE_LIMIT` | float64 | `10.0` | Messages per second |
| `GATEWAY_PUBLISH_BURST` | int | `100` | Burst capacity |
| `GATEWAY_MAX_PUBLISH_SIZE` | int | `65536` | Max message size (64KB = protocol.DefaultMaxPublishSize) |
| `GATEWAY_AUTH_REFRESH_RATE_INTERVAL` | time.Duration | `30s` | Auth refresh settings |
| `GATEWAY_AUTH_VALIDATION_TIMEOUT` | time.Duration | `5s` | Auth validation timeout for intercepting auth refresh operations |
| `REVOCATION_MAX_LIFETIME` | time.Duration | `24h` | Max time a revocation entry lives when exp is not provided |
| `GATEWAY_SHUTDOWN_TIMEOUT` | time.Duration | `30s` | Graceful shutdown timeout |
| `WS_INTERNAL_SECRET` | string | — | InternalSecret is forwarded verbatim as X-Sukko-Internal-Secret to ws-server. No envDefault — empty string is valid when ws-server WS_INTERNAL_SECRET_ENABLED=false. redact:"true" prevents the /config endpoint from exposing this value. |
| `SERVER_GRPC_ADDR` | string | `localhost:3006` | ws-server gRPC address for RealtimeService |
| `PUSH_GRPC_ADDR` | string | `localhost:3008` | Push service gRPC address for PushService (RegisterDevice, UnregisterDevice, GetVAPIDKey) |
| `SSE_KEEPALIVE_INTERVAL` | time.Duration | `45s` | SSE keepalive comment interval (prevents proxy timeouts) |
| `GATEWAY_CORS_ORIGINS` | []string | `*` | CORS allowed origins (* = all, production: restrict) |
| `GATEWAY_CHANNEL_RULES_CACHE_TTL` | time.Duration | `1m` | Channel rules provider cache TTLs |
| `GATEWAY_REGISTRY_QUERY_TIMEOUT` | time.Duration | `5s` |  |

## Core

| Variable | Type | Default | Description |
|----------|------|---------|-------------|
| `WS_ADDR` | string | `:3002` | Server basics |
| `WS_NUM_SHARDS` | int | `1` | Number of server shards |
| `WS_BASE_PORT` | int | `3002` | Base port for shard binding (3002, 3003, ...) |
| `WS_LB_ADDR` | string | `:3005` | Load balancer listen address |
| `SERVER_GRPC_PORT` | int | `3006` | gRPC server port for RealtimeService (SSE Subscribe + REST Publish) |
| `KAFKA_CONSUMER_ENABLED` | bool | `true` | KafkaConsumerEnabled controls whether the Kafka consumer pool is started. When false, the consumer pool is not created — no consumer group join, no message consumption. WebSocket connections still work (connection-only mode for loadtesting). Default: true |
| `KAFKA_DEFAULT_PARTITIONS` | int | `1` | Kafka Topic Defaults (for on-demand topic creation by KafkaBackend) |
| `KAFKA_DEFAULT_REPLICATION_FACTOR` | int | `1` |  |
| `WS_MEMORY_LIMIT` | int64 | `536870912` | 512MB |
| `WS_MAX_CONNECTIONS` | int | `500` | Capacity |
| `WS_MAX_KAFKA_RATE` | int | `1000` | Kafka message consumption rate |
| `WS_MAX_BROADCAST_RATE` | int | `25` |  |
| `WS_MAX_GOROUTINES` | int | `100000` |  |
| `WS_RATE_LIMIT_BURST_MULTIPLIER` | int | `2` | Burst capacity as a multiple of the steady-state rate |
| `WS_CLIENT_MSG_BURST_LIMIT` | int | `100` | Per-client burst capacity |
| `WS_CLIENT_MSG_RATE_PER_SEC` | float64 | `10` | Per-client sustained rate/sec |
| `CONN_RATE_LIMIT_ENABLED` | bool | `true` | Connection rate limiting (DoS protection) |
| `CONN_RATE_LIMIT_IP_BURST` | int | `100` |  |
| `CONN_RATE_LIMIT_IP_RATE` | float64 | `100.0` |  |
| `CONN_RATE_LIMIT_GLOBAL_BURST` | int | `300` |  |
| `CONN_RATE_LIMIT_GLOBAL_RATE` | float64 | `50.0` |  |
| `WS_CPU_REJECT_THRESHOLD` | float64 | `60.0` | Upper: start rejecting above this % |
| `WS_CPU_REJECT_THRESHOLD_LOWER` | float64 | `0` | Lower: stop rejecting below this % (0 = auto: upper - 10) |
| `WS_CPU_PAUSE_THRESHOLD` | float64 | `70.0` | Upper: pause Kafka above this % |
| `WS_CPU_PAUSE_THRESHOLD_LOWER` | float64 | `0` | Lower: resume Kafka below this % (0 = auto: upper - 10) |
| `WS_CPU_EWMA_BETA` | float64 | `0.8` | EWMA decay factor for CPU smoothing (0-1, higher = smoother) |
| `TCP_LISTEN_BACKLOG` | int | `2048` | TCP accept queue size (0 = Go default ~128) |
| `METRICS_INTERVAL` | time.Duration | `15s` | Monitoring |
| `CPU_POLL_INTERVAL` | time.Duration | `1s` | CPU Polling Interval (for protection decisions) Separate from MetricsInterval to allow faster CPU spike detection while keeping full metrics reporting (memory, goroutines) at a reasonable interval.  CPU polling is used by: - ShouldPauseKafka() - backpressure when CPU &gt; 80% - ShouldAcceptConnection() - reject connections when CPU &gt; 75%  Trade-off: 1s polling = 0.1% CPU overhead, but 15x faster spike detection |
| `ALERT_ENABLED` | bool | `false` | Alerting |
| `ALERT_MIN_LEVEL` | string | `WARNING` |  |
| `ALERT_SLACK_WEBHOOK_URL` | string | — |  |
| `ALERT_SLACK_CHANNEL` | string | — |  |
| `ALERT_SLACK_USERNAME` | string | `AlertBot` |  |
| `ALERT_SLACK_TIMEOUT` | time.Duration | `5s` |  |
| `ALERT_RATE_LIMIT_WINDOW` | time.Duration | `5m` |  |
| `ALERT_RATE_LIMIT_MAX` | int | `3` |  |
| `ALERT_CONSOLE_ENABLED` | bool | `false` |  |
| `VALKEY_ADDRS` | []string | `localhost:6379` | Valkey Configuration (for BroadcastBus when BROADCAST_TYPE=valkey) Supports both self-hosted Sentinel (3 addresses) and single instance (1 address) |
| `VALKEY_MASTER_NAME` | string | `mymaster` |  |
| `VALKEY_PASSWORD` | string | — |  |
| `VALKEY_DB` | int | `0` |  |
| `VALKEY_CHANNEL` | string | `ws.broadcast` |  |
| `WS_SLOW_CLIENT_MAX_ATTEMPTS` | int | `3` | Slow Client Detection  When a client's send buffer is full, the server tracks consecutive failed send attempts. After SlowClientMaxAttempts consecutive failures, the client is disconnected.  Industry comparison:   - Coinbase: 2 attempts (aggressive)   - Binance: No automatic disconnect (relies on ping timeout)   - FIX protocol: 5 second timeout (lenient)  Default: 3 (balanced - tolerates brief hiccups, disconnects persistent slow clients) Range: 1-10 (1 = aggressive, 10 = very lenient) |
| `WS_CLIENT_SEND_BUFFER_SIZE` | int | `512` | Client Send Buffer Size Controls the per-client send channel buffer (memory vs slow-client tolerance trade-off)  Buffer sizing at 125 msg/sec broadcast rate (25 msg/sec × 5 channel subscriptions): - 512 slots: ~256KB/client, 4.1s buffer, ~3.5GB heap at 13.5K clients - 1024 slots: ~512KB/client, 8.2s buffer, ~6.9GB heap at 13.5K clients  Trade-off: - Smaller buffer = less memory = less GC pressure = lower CPU spikes - Larger buffer = more tolerance for slow clients and network hiccups  Default: 512 (reduced from 1024 to cut heap size by 50%, reduce GC pressure) Production guidance: Start with 512, increase to 768 or 1024 if cascade disconnects occur |
| `WS_GAP_NOTIFY_BUFFER_SIZE` | int | `8` | GapNotifyBufferSize is the per-client buffer for low-priority gap notifications. Must be 1-MaxGapNotifyBufferSize (64); 0 would make the channel unbuffered (blocks Broadcast hot path). Keep small relative to ClientSendBufferSize — gaps are low-frequency out-of-band events. Drops beyond capacity are counted via ws_gap_notifications_dropped_total\{reason="buffer_full"\}. |
| `WS_REPLAY_RATE_LIMIT_INTERVAL` | time.Duration | `10s` | ReplayRateLimitInterval is the minimum time between live replay requests per (client, channel) pair. Prevents unbounded Kafka reads per client. |
| `BROADCAST_TYPE` | string | `valkey` | Broadcast Bus Configuration  BroadcastType: Backend for inter-instance messaging ("valkey") - valkey: Redis-compatible Pub/Sub (default, ~1-2ms latency, required for message history) |
| `VALKEY_TLS_ENABLED` | bool | `false` | Valkey Broadcast Bus TLS (for managed Valkey/Redis: ElastiCache, Memorystore, Upstash, etc.) |
| `VALKEY_TLS_INSECURE` | bool | `false` |  |
| `VALKEY_TLS_CA_PATH` | string | — |  |
| `TOPIC_REFRESH_INTERVAL` | time.Duration | `60s` | TopicRefreshInterval is how often to re-sync tenant topics from the registry. Safety net — primary updates come from the gRPC stream. |
| `WS_PONG_WAIT` | time.Duration | `60s` | WebSocket Ping/Pong Configuration  These settings control the keep-alive mechanism between the shard and clients. In a proxied architecture (gateway → ws-server → shard), pings travel through multiple hops, requiring a larger buffer than direct connections.  Buffer = PongWait - PingPeriod The buffer must accommodate the round-trip time through all proxies. With 8 network hops (local Kind + port-forward), 15+ seconds is recommended.  Example with defaults (60s/45s):   - Shard sends ping at t=0   - Ping travels: Shard → ShardProxy → ws-server → Gateway → Client   - Client sends pong   - Pong travels: Client → Gateway → ws-server → ShardProxy → Shard   - If pong arrives by t=60s, connection is healthy   - 15 second buffer for network latency, GC pauses, etc.  Common issues:   - Buffer too small (&lt;5s): Connections drop during GC pauses or network hiccups   - PingPeriod &gt;= PongWait: Invalid, ping would always timeout |
| `WS_PING_PERIOD` | time.Duration | `45s` |  |
| `WS_WRITE_WAIT` | time.Duration | `5s` | WriteWait is the timeout for WebSocket write operations. 5s is sufficient for local writes; network latency is handled by TCP. |
| `WS_REPLAY_TIMEOUT` | time.Duration | `5s` | Handler timeouts |
| `WS_PUBLISH_TIMEOUT` | time.Duration | `5s` |  |
| `WS_MAX_REPLAY_MESSAGES` | int | `100` |  |
| `WS_TOPIC_CREATION_TIMEOUT` | time.Duration | `30s` | Topic/backend timeouts |
| `WS_SHARD_DIAL_TIMEOUT` | time.Duration | `10s` | Orchestration |
| `WS_SHARD_MESSAGE_TIMEOUT` | time.Duration | `60s` |  |
| `WS_METRICS_AGGREGATION_INTERVAL` | time.Duration | `5s` |  |
| `WS_SHUTDOWN_GRACE_PERIOD` | time.Duration | `30s` | Shutdown |
| `WS_SHUTDOWN_CHECK_INTERVAL` | time.Duration | `1s` |  |
| `WS_METRICS_COLLECT_INTERVAL` | time.Duration | `2s` | Internal monitoring |
| `WS_MEMORY_MONITOR_INTERVAL` | time.Duration | `30s` |  |
| `WS_MEMORY_WARNING_PERCENT` | int | `80` |  |
| `WS_MEMORY_CRITICAL_PERCENT` | int | `90` |  |
| `WS_BUFFER_SAMPLE_INTERVAL` | time.Duration | `10s` |  |
| `WS_BUFFER_MAX_SAMPLES` | int | `100` |  |
| `WS_BUFFER_HIGH_SATURATION_PERCENT` | int | `90` |  |
| `WS_BUFFER_POPULATION_WARN_PERCENT` | int | `25` |  |
| `CONN_RATE_LIMIT_IP_TTL` | time.Duration | `5m` | Connection rate limiter internals |
| `CONN_RATE_LIMIT_CLEANUP_INTERVAL` | time.Duration | `1m` |  |
| `BROADCAST_BUFFER_SIZE` | int | `256` | Broadcast bus |
| `BROADCAST_SHUTDOWN_TIMEOUT` | time.Duration | `5s` |  |
| `VALKEY_WRITE_TIMEOUT` | time.Duration | `3s` | Valkey broadcast tuning |
| `VALKEY_PUBLISH_TIMEOUT` | time.Duration | `100ms` |  |
| `VALKEY_STARTUP_PING_TIMEOUT` | time.Duration | `5s` |  |
| `VALKEY_RECONNECT_INITIAL_BACKOFF` | time.Duration | `100ms` |  |
| `VALKEY_RECONNECT_MAX_BACKOFF` | time.Duration | `30s` |  |
| `VALKEY_RECONNECT_MAX_ATTEMPTS` | int | `10` |  |
| `VALKEY_HEALTH_CHECK_INTERVAL` | time.Duration | `10s` |  |
| `VALKEY_HEALTH_CHECK_TIMEOUT` | time.Duration | `5s` |  |
| `VALKEY_PUBLISH_STALENESS_THRESHOLD` | time.Duration | `60s` | Log warning if no publish within this window |
| `KAFKA_BATCH_SIZE` | int | `50` | Kafka consumer tuning |
| `KAFKA_BATCH_TIMEOUT` | time.Duration | `10ms` |  |
| `KAFKA_FETCH_MAX_WAIT` | time.Duration | `500ms` | Kafka consumer transport tuning |
| `KAFKA_FETCH_MIN_BYTES` | int32 | `1` |  |
| `KAFKA_FETCH_MAX_BYTES` | int32 | `10485760` |  |
| `KAFKA_SESSION_TIMEOUT` | time.Duration | `30s` |  |
| `KAFKA_REBALANCE_TIMEOUT` | time.Duration | `60s` |  |
| `KAFKA_REPLAY_FETCH_MAX_BYTES` | int32 | `5242880` |  |
| `KAFKA_BACKPRESSURE_CHECK_INTERVAL` | time.Duration | `100ms` |  |
| `KAFKA_COMMIT_ON_REVOKE_TIMEOUT` | time.Duration | `5s` | KafkaCommitOnRevokeTimeout controls how long OnPartitionsRevoked waits for CommitMarkedOffsets to complete before returning. Must be &gt; 0 and &lt; KafkaRebalanceTimeout. |
| `KAFKA_AUTO_COMMIT_INTERVAL` | time.Duration | `5s` | KafkaAutoCommitInterval controls the background auto-commit interval. Must be &gt;= MinAutoCommitInterval (100ms). Franz-go enforces this floor; pre-validation here produces a clear error per Constitution §I. |
| `KAFKA_PRODUCER_BATCH_MAX_BYTES` | int | `1048576` | Kafka producer tuning |
| `KAFKA_PRODUCER_MAX_BUFFERED_RECORDS` | int | `10000` |  |
| `KAFKA_PRODUCER_RECORD_RETRIES` | int | `8` |  |
| `KAFKA_PRODUCER_CB_TIMEOUT` | time.Duration | `30s` |  |
| `KAFKA_PRODUCER_CB_MAX_FAILURES` | int | `5` |  |
| `KAFKA_PRODUCER_CB_HALF_OPEN_REQS` | int | `1` |  |
| `WS_ROUTING_FANOUT_WORKERS` | int | `4` | Routing fan-out worker pool |
| `WS_ROUTING_FANOUT_QUEUE_SIZE` | int | `256` |  |
| `WS_ROUTING_DLQ_MAX_RETRIES` | int | `3` | Dead-letter queue retry pool |
| `WS_ROUTING_DLQ_BASE_DELAY` | time.Duration | `100ms` |  |
| `WS_ROUTING_DLQ_MAX_DELAY` | time.Duration | `5s` |  |
| `WS_ROUTING_DLQ_RETRY_WORKERS` | int | `4` |  |
| `-` | string | — | PodID is the resolved pod identity, populated by Normalize() from PodIdentityConfig.PodID(). Not an env var — configure via PodIdentityConfig (SUKKO_POD_ID / WS_POD_ID). Kept as a plain string so hot-path code (history writer, registry) avoids repeated method calls. |
| `WS_MAX_CHANNELS_PER_CLIENT` | int | `100` | MaxChannelsPerClient limits the number of concurrent channel subscriptions per client. Prevents channel-fan-out attacks. Validated against MaxChannelsPerClientLimit (1000). |
| `WS_HISTORY_ENABLED` | bool | `false` | Message History (two-tier: Valkey Streams ring buffer + Kafka fallback) |
| `WS_HISTORY_BUFFER_DEPTH` | int | `1000` | HistoryBufferDepth is the Valkey Streams MAXLEN ~ per-channel (XADD MAXLEN ~). |
| `WS_HISTORY_TTL` | time.Duration | `24h` | HistoryTTL is the per-stream key expiry (EXPIRE) applied on every write. |
| `WS_HISTORY_WRITER_BUFFER` | int | `10000` | HistoryWriterBuffer is the historyWriter workChan capacity (messages in-flight before drops). |
| `WS_HISTORY_MAX_LIMIT` | int | `100` | HistoryMaxLimit is the maximum number of entries a client may request per history call. |
| `WS_HISTORY_DELIVERY_TIMEOUT` | time.Duration | `30s` | HistoryDeliveryTimeout is the per-request deadline for XREVRANGE + Kafka fallback. |
| `WS_HISTORY_WRITER_PIPELINE_BATCH` | int | `50` | HistoryWriterPipelineBatch is the max number of XADD commands per DoMulti pipeline. |
| `WS_HISTORY_WRITER_LOCK_TTL` | time.Duration | `10s` | HistoryWriterLockTTL is the distributed lock TTL for the active historyWriter pod. |
| `-` | int64 | — | HistoryWriterLockTTLMs is pre-computed from HistoryWriterLockTTL in Normalize(). Used by Valkey SET PX (milliseconds). Not an env var. |
| `WS_HISTORY_WRITER_RESTART_INITIAL_BACKOFF` | time.Duration | `500ms` | HistoryWriterRestartInitialBackoff is the starting delay before re-entering runOnce after a failure. |
| `WS_HISTORY_WRITER_RESTART_MAX_BACKOFF` | time.Duration | `30s` | HistoryWriterRestartMaxBackoff caps the exponential backoff for historyWriter restarts. |
| `WS_HISTORY_VALKEY_CMD_TIMEOUT` | time.Duration | `2s` | HistoryValkeyCmdTimeout is the per-command timeout for history Valkey operations (XADD, XREVRANGE, EXPIRE). |
| `WS_HISTORY_MAX_CONSECUTIVE_LOCK_FAILURES` | int | `3` | HistoryMaxConsecutiveLockFailures is the number of consecutive heartbeat lock failures before the historyWriter exits runOnce and re-enters the supervised restart loop. |
| `WS_HISTORY_WRITER_HEARTBEAT_INTERVAL` | time.Duration | `3s` | HistoryWriterHeartbeatInterval is the ticker interval for lock renewal and bus health checks. |
| `WS_CONNECTIONS_REGISTRY_ENABLED` | bool | `false` | Connections Registry (Connections Management API — Pro edition) When false, registryWriter and adminListeners are skipped entirely. X-Sukko-Tenant-ID is still read unconditionally (tenantHooks and channel subscription require it regardless). |
| `WS_CONNECTIONS_REGISTRY_TTL` | time.Duration | `120s` | ConnectionsRegistryTTL is the Valkey EXPIRE duration for each connection hash. Heartbeats renew the TTL; if a pod dies without heartbeat, entries expire after this. |
| `WS_CONNECTIONS_REGISTRY_BUFFER` | int | `1024` | ConnectionsRegistryBuffer is the internal channel buffer for registry events (connect, disconnect, subscribe, unsubscribe, heartbeat). |
| `WS_CONNECTIONS_REGISTRY_FLUSH_INTERVAL` | time.Duration | `5s` | ConnectionsRegistryFlushInterval is the tick interval for flushing buffered events to Valkey. |
| `WS_CONNECTIONS_REGISTRY_HEARTBEAT_INTERVAL` | time.Duration | `30s` | ConnectionsRegistryHeartbeatInterval is how often the registry writer renews TTLs and writes health keys. |
| `WS_CONNECTIONS_REGISTRY_RESTART_INITIAL_BACKOFF` | time.Duration | `100ms` | ConnectionsRegistryRestartInitialBackoff is the initial restart delay after a registryWriter failure. |
| `WS_CONNECTIONS_REGISTRY_RESTART_MAX_BACKOFF` | time.Duration | `30s` | ConnectionsRegistryRestartMaxBackoff is the maximum restart delay after repeated failures. |
| `WS_CONNECTIONS_REGISTRY_SHUTDOWN_DRAIN_TIMEOUT` | time.Duration | `100ms` | ConnectionsRegistryShutdownDrainTimeout is how long to wait draining buffered events on shutdown. 0 = skip drain entirely (useful for tests). |
| `WS_ADMIN_CHANNEL_SUBSCRIBE_TIMEOUT` | time.Duration | `10s` | AdminChannelSubscribeTimeout is the timeout for each shard's synchronous SUBSCRIBE call at startup. |
| `WS_INTERNAL_SECRET_ENABLED` | bool | `false` | InternalSecretEnabled controls whether ws-server validates X-Sukko-Internal-Secret. When true, WS_INTERNAL_SECRET must be non-empty or startup fails. |
| `WS_INTERNAL_SECRET` | string | — | InternalSecret is the shared secret forwarded by the gateway as X-Sukko-Internal-Secret. redact:"true" prevents the /config endpoint from exposing this value. |

## Provisioning

| Variable | Type | Default | Description |
|----------|------|---------|-------------|
| `ANALYTICS_PARTMAN_INTERVAL` | time.Duration | `15m` | Analytics — provisioning-only fields |
| `ANALYTICS_SSE_MAX_CONNS` | int | `10` |  |
| `PROVISIONING_KAFKA_BROKERS` | string | `localhost:19092` | Kafka brokers — used by the provisioning service for topic management (create/delete). Uses a provisioning-specific env var to avoid collision with the ws-server's KAFKA_BROKERS (defined in MessageBackendConfig). In-cluster default: &lt;release&gt;-redpanda:9092. |
| `PROVISIONING_VALKEY_ADDRS` | []string | — | Valkey node addresses for the connections registry (token revocation, bulk disconnect pub/sub). No envDefault — must always be set explicitly. Format: `host:port`. Comma-separated for cluster mode (e.g. `valkey:6379`). |
| `PROVISIONING_ADDR` | string | `:8080` | Server |
| `GRPC_PORT` | int | `9090` | gRPC — internal service-to-service communication port |
| `ADMIN_BOOTSTRAP_KEY` | string | — | Admin Authentication — bootstrap key for first admin registration (base64-encoded Ed25519 public key) |
| `DEFAULT_PARTITIONS` | int | `3` | Topic Defaults |
| `DEFAULT_RETENTION_MS` | int64 | `604800000` | 7 days |
| `MAX_TOPICS_PER_TENANT` | int | `50` | Quotas (defaults per tenant) |
| `MAX_PARTITIONS_PER_TENANT` | int | `200` |  |
| `MAX_STORAGE_BYTES` | int64 | `10737418240` | 10GB |
| `PRODUCER_BYTE_RATE` | int64 | `10485760` | 10MB/s |
| `CONSUMER_BYTE_RATE` | int64 | `52428800` | 50MB/s |
| `DEPROVISION_GRACE_DAYS` | int | `30` | Tenant Lifecycle |
| `LIFECYCLE_CHECK_INTERVAL` | time.Duration | `1h` |  |
| `LIFECYCLE_MANAGER_ENABLED` | bool | `true` |  |
| `MAX_ROUTING_RULES_PER_TENANT` | int | `100` | Max routing rules per tenant |
| `MAX_TOPICS_PER_RULE` | int | `10` | Max topics per routing rule |
| `ROUTING_DLQ_TOPIC_PARTITIONS` | int | `1` | Partitions for dead-letter topics |
| `ROUTING_DLQ_TOPIC_RETENTION_MS` | int64 | `604800000` | Retention for dead-letter topics (7 days) |
| `INFRA_TOPIC_REPLICATION_FACTOR` | int | `1` | Replication factor for all infrastructure topics (DLQ + default); use int — caarlos0/env v11 does not handle int16; cast to int16 at CreateTopic call sites |
| `API_RATE_LIMIT_PER_MIN` | int | `60` | Rate Limiting |
| `KEY_REGISTRY_REFRESH_INTERVAL` | time.Duration | `1m` | Key Registry (for JWT validation in API mode with auth enabled) |
| `KEY_REGISTRY_QUERY_TIMEOUT` | time.Duration | `5s` |  |
| `SHUTDOWN_TIMEOUT` | time.Duration | `30s` | Graceful shutdown timeout |
| `CORS_ALLOWED_ORIGINS` | []string | `http://localhost:3000` | CORS settings |
| `CORS_MAX_AGE` | int | `3600` |  |
| `PROVISIONING_MAX_TENANTS_FETCH_LIMIT` | int | `10000` | Provisioning-specific externalized constants |
| `PROVISIONING_DELETION_TIMEOUT` | time.Duration | `5m` |  |
| `SLUG_RENAME_TOPIC_HOLD_PERIOD` | time.Duration | `168h` | SlugRenameTopicHoldPeriod is how long old Kafka topics and ACLs are retained after a slug rename. During this window the old slug is still accepted by the JWT grace-period check. Must be &gt;= MinSlugRenameHoldPeriod (24h). Default: 168h (7 days). |
| `PROVISIONING_TOKEN_REVOCATION_MAX_LIFETIME` | time.Duration | `1h` | TokenRevocationMaxLifetime is the maximum duration a revoked token entry is retained before the background prune loop removes it. Must be &gt; 0. Default: 1h. |
| `PROVISIONING_BULK_DISCONNECT_CONCURRENCY` | int | `10` | BulkDisconnectConcurrency controls the maximum number of concurrent pub/sub publishes during a bulk DELETE /connections request. |
| `MAX_WEBHOOKS_PER_TENANT` | int | `10` |  |
| `WEBHOOK_DOWNGRADE_POLL_INTERVAL` | time.Duration | `5m` |  |
| `WEBHOOK_WORKER_GRPC_PORT` | int | `9091` | WebhookWorkerGRPCPort is the dedicated port for the webhook-worker gRPC service. Separate from GRPC_PORT (ProvisioningInternalService) so auth is enforced at the listener level rather than via FullMethod filtering inside a shared interceptor. Direction: webhook-worker → provisioning (WebhookWorkerService). |
| `WEBHOOK_WORKER_GRPC_ADDR` | string | — | WebhookWorkerGRPCAddr is the address provisioning dials to call the worker's WebhookWorkerInternalService.TestDeliver RPC (reverse direction: provisioning → worker). No envDefault — required only when EditionHasFeature(Webhooks); validated conditionally. |

## Tester

| Variable | Type | Default | Description |
|----------|------|---------|-------------|
| `TESTER_PORT` | int | `8090` |  |
| `TESTER_AUTH_TOKEN` | string | — | no default — must be explicitly set for production |
| `GATEWAY_URL` | string | `ws://localhost:3000` |  |
| `PROVISIONING_URL` | string | `http://localhost:8080` |  |
| `TESTER_SIGNING_KEY_FILE` | string | — | License reload suite — Ed25519 private key for signing test license keys |
| `TESTER_ADMIN_KEY_FILE` | string | — | Admin keypair for remote mode (required when targeting a deployed provisioning service). When unset, the tester generates an ephemeral keypair per test run (local dev mode only). |
| `TESTER_ADMIN_KEY_ID` | string | `bootstrap-0` | AdminKeyID is the kid embedded in admin JWTs; must match BootstrapAdminKeyID unless a custom key was registered with provisioning under a different ID. envDefault must stay in sync with provauth.BootstrapAdminKeyID; enforced by TestTesterConfig_DefaultAdminKeyID_EqualsBootstrapConstant. |
| `TESTER_JWT_LIFETIME` | time.Duration | `15m` | JWT auth configuration |
| `TESTER_JWT_REFRESH_BEFORE` | time.Duration | `2m` |  |
| `TESTER_KEY_EXPIRY` | time.Duration | `24h` |  |
| `TESTER_AUTH_MODE` | runner.AuthMode | `jwt` | Auth mode configuration (FR-001, FR-002, SC-001) AuthMode controls the credential mode for connections: jwt, api-key, upgrade, or mixed. |
| `TESTER_API_KEY` | string | — | APIKey is a static pre-provisioned API key for api-key, upgrade, and mixed modes. Tagged redact:"true" so the /config endpoint never echoes it (§IX). |
| `TESTER_AUTH_MIX_RATIO` | float64 | `0.5` |  |
| `TESTER_AUTH_UPGRADE_TIMEOUT` | time.Duration | `10s` |  |
| `TESTER_GATEWAY_METRICS_URL` | string | — | Gateway metrics scraping for revocation load suites. GatewayMetricsURL is the Prometheus metrics endpoint of the gateway pod. When empty, metric-drift checks are skipped (not an error — recorded as skipped_checks). |
| `TESTER_GATEWAY_METRICS_INTERVAL` | time.Duration | `30s` |  |

